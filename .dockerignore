# Version control
.git/
.gitignore

# Python cache
__pycache__/
*.py[codz]
*.pyo
.ruff_cache/
.pytest_cache/
*.egg-info/

# Dev tooling and notebooks
.vscode/
.logfire/
notebooks/
tests/
eval/

# Secrets — must never enter the image; set these as HF Space secrets instead
.env

# Poetry / build artefacts — deps are installed from requirements.txt
pyproject.toml
poetry.lock

# Data: exclude everything under data. Resets are done via Neon branches.
data/

# Docs and local notes
CLAUDE.md
AGENTS.md
todo.txt
