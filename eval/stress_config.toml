[neon]
# Neon project ID (visible in the console URL: console.neon.tech/app/projects/<project_id>).
project_id = "orange-art-94985227"
# Neon branch name to restore to its parent baseline before each stress run.
branch_name = "development"
# Number of retry attempts when the branch is locked (HTTP 423).
lock_retry_attempts = 8
# Seconds to wait between lock retry attempts.
lock_retry_delay_s = 5.0

[orchestration]
# Time in seconds to wait for OrchestrationManager readiness.
ready_timeout_s = 90.0

[stress.workload]
# Number of concurrent simulated users.
users = 50
# Number of operations per user.
ops_per_user = 5
# Maximum number of users allowed to run concurrently.
max_concurrency = 50
# Relative weight for BOOK operations in the op-type mix (need not sum to 1).
book_weight = 0.5
# Relative weight for MODIFY operations in the op-type mix.
modify_weight = 0.25
# Relative weight for CANCEL operations in the op-type mix.
cancel_weight = 0.25

[stress.targets]
# Nights per booking when generating targets.
stay_nights = 2
# Number of available targets to precompute.
num_targets = 500
# Size of the hot contention target subset.
hot_target_count = 10
# Probability of selecting a hot target vs any target (0.0-1.0).
hot_target_probability = 0.8
# Search start date for availability targets (YYYY-MM-DD).
start_date = "2025-02-01"
# Search horizon in days from the start date.
horizon_days = 90

[stress.db]
# Maximum size of the stress-test database pool.
pool_max = 10
# Retry attempts on transient DB connection errors.
db_retry_attempts = 3
# Base delay in seconds between DB retry attempts.
db_retry_delay_s = 2.0
# Maximum entries in reconciliation failure detail lists.
reconcile_max_detail = 10

[stress.output]
# Base output directory for stress artifacts.
output_dir = "eval/outputs"
# Directory for stress run logs.
log_dir = "eval/logs"
