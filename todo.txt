Database user with limited permissions
Once the LLM asked which hotel the user was referring to when it should have called the retriever.
Add note at bottom of screen that the bot may only be returning limited results and there may be more available.
Add multiple user support
Make sure notebooks are well-annotated.